import React from 'react';
import { Card } from 'antd';
import styles from './index.less';

const PaySlips = () => {
  return (
    <Card title="Pay Slips" className={styles.PaySlips}>
      pay slips
    </Card>
  );
};

export default PaySlips;
